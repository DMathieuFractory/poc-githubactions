name: Dependency Update Check

on:
  push:
    branches:
      - feat/project_vue
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

jobs:
  check-deps:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repo
      - uses: actions/checkout@v4

      # 2️⃣ Setup Node.js
      - uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3️⃣ Check dependencies in root
      - name: Check dependencies (root)
        id: check_root
        run: |
          npm install
          npm outdated --json > outdated_root.json || true
          echo "has_major=false" >> $GITHUB_OUTPUT
          jq -r 'to_entries[] | "\(.key) \(.value.current) \(.value.latest)"' outdated_root.json | while read pkg cur lat; do
            cur_major=$(echo $cur | cut -d. -f1 | tr -d '[:alpha:]')
            lat_major=$(echo $lat | cut -d. -f1 | tr -d '[:alpha:]')
            if [[ "$cur_major" != "$lat_major" ]]; then
              echo "MAJOR update found in root: $pkg ($cur → $lat)"
              echo "has_major=true" >> $GITHUB_OUTPUT
            fi
          done

      # 4️⃣ Check dependencies in ./app
      - name: Check dependencies (app)
        id: check_app
        working-directory: ./app
        run: |
          npm install
          npm outdated --json > outdated_app.json || true
          echo "has_major=false" >> $GITHUB_OUTPUT
          jq -r 'to_entries[] | "\(.key) \(.value.current) \(.value.latest)"' outdated_app.json | while read pkg cur lat; do
            cur_major=$(echo $cur | cut -d. -f1 | tr -d '[:alpha:]')
            lat_major=$(echo $lat | cut -d. -f1 | tr -d '[:alpha:]')
            if [[ "$cur_major" != "$lat_major" ]]; then
              echo "MAJOR update found in app: $pkg ($cur → $lat)"
              echo "has_major=true" >> $GITHUB_OUTPUT
            fi
          done

      # 5️⃣ Warning dans Actions si MAJOR détectée
      - name: Warn about major updates
        if: steps.check_root.outputs.has_major == 'true' || steps.check_app.outputs.has_major == 'true'
        run: |
          echo "::warning::🚨 Major dependency updates detected! Check GitHub issues."

      # 6️⃣ Create GitHub issue if MAJOR detected
      - name: Create issue if major update
        if: steps.check_root.outputs.has_major == 'true' || steps.check_app.outputs.has_major == 'true'
        uses: peter-evans/create-issue-from-file@v6
        with:
          title: "🚨 Major dependency update detected"
          content-filepath: outdated_root.json
          labels: dependencies